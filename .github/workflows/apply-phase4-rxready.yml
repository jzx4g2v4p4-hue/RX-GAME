name: Apply RxReady phase 4

on:
  push:
    branches:
      - main
    paths:
      - scripts/apply-phase4-rxready.cjs
      - .github/workflows/apply-phase4-rxready.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply RxReady phase 4 patch
        run: node scripts/apply-phase4-rxready.cjs
      - name: Commit source update and remove helper files
        run: |
          rm -f scripts/apply-phase4-rxready.cjs
          rm -f .github/workflows/apply-phase4-rxready.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/RxReady.jsx scripts/apply-phase4-rxready.cjs .github/workflows/apply-phase4-rxready.yml
          git commit -m "Add career mode consequences"
          git push
